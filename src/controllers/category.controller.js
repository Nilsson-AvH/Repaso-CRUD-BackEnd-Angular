const createCategory = (req, res) => {
    console.log('Registra una categoria desde controlador');
    res.json({msg: 'Registra una categoria desde controlador'});
};

export {
    createCategory
};
